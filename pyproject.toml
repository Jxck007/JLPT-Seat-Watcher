[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "jlpt-seat-watcher"
version = "1.0.0"
description = "Adapter-driven, fault-tolerant website monitor"
readme = "README.md"
requires-python = ">=3.12"
license = {file = "LICENSE"}
authors = [{name = "Jegadeesh (Jack) Nandakumar"}]
dependencies = [
  "beautifulsoup4==4.13.4",
  "filelock==3.18.0",
  "playwright==1.54.0",
  "python-dotenv==1.1.1",
  "PyYAML==6.0.2",
  "requests==2.32.4",
]

[project.scripts]
watchtower = "watchtower.cli:main"
jlpt-seat-watcher = "watchtower.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.black]
line-length = 88
target-version = ["py312"]

[tool.isort]
profile = "black"
line_length = 88

[tool.ruff]
target-version = "py312"
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM", "RUF"]

[tool.mypy]
python_version = "3.12"
strict = true
packages = ["watchtower"]
mypy_path = "src"

[tool.pytest.ini_options]
addopts = "--strict-markers --strict-config --cov=watchtower --cov-report=term-missing --cov-fail-under=85"
testpaths = ["tests"]
markers = ["live: accesses the live JLPT website"]

[tool.coverage.run]
branch = true
source = ["watchtower"]
